#pragma once
#include <SDL.h>
#include <stdio.h>
#include <SDL_image.h>
// imported the SDL Libraries & other STDIO (maybe needed)
using namespace std;

class Piece{
    public:
        int xyz;
    private:

};

class Pawn{
    public:

    private:
};